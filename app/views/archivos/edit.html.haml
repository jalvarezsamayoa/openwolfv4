- content_for :head do
  = javascript_include_tag 'combobox.js', 'archivos.js'

- @forview = "EDIT"
= render :partial=>"breadcrumbs"

.mantform
  %h2
    Editar
    = Archivo.model_name.human

  = simple_form_for @archivo do |form|

    = render :partial => 'form', :locals => {:form => form}
    = buttons_update(archivos_path)