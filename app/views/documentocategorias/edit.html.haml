.mantform
  %h2
    Editar #{Documentocategoria.model_name.human}
  = simple_form_for @documentocategoria do |form|

    = render :partial => 'form', :locals => {:form => form}
    = buttons_update(documentocategorias_path)
