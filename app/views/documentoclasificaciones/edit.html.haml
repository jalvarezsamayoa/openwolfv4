.mantform
  %h2
    Editar #{Documentoclasificacion.model_name.human}
  = simple_form_for @documentoclasificacion do |form|

    = render :partial => 'form', :locals => {:form => form}
    = buttons_update(documentoclasificaciones_path)
