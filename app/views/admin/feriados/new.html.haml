#page-title.clearfix
  %h1= "Nuevo #{Feriado.model_name.human}"
  = render :partial => "shared/breadcrumbs"

.show.widget
  .widget-header
    %h3 Nuevo Feriado
  .widget-content
    = simple_form_for([:admin,@feriado], :html => {:class => 'form-horizontal'}) do |form|
      = render :partial => 'form', :locals => {:form => form}
      .form-actions
        = buttons_create(admin_feriados_path)