#page-title.clearfix
  %h1= "Editar #{Feriado.model_name.human}"
  = render :partial => "shared/breadcrumbs"

.show.widget
  .widget-header
    %h3 Editar Feriado
  .widget-content
    = simple_form_for([:admin,@feriado], :html => {:class => 'form-horizontal'}) do |form|
      = render :partial => 'form', :locals => {:form => form}
      .form-actions
        = buttons_update(admin_feriados_path)