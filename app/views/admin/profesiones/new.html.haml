#page-title.clearfix
  %h1= "Nuevo #{Profesion.model_name.human}"
  = render :partial => "shared/breadcrumbs"

.show.widget
  .widget-header
    %h3 Nuevo Profesion
  .widget-content
    = simple_form_for([:admin,@profesion], :html => {:class => 'form-horizontal'}) do |form|
      = render :partial => 'form', :locals => {:form => form}
      .form-actions
        = buttons_create(admin_profesiones_path)