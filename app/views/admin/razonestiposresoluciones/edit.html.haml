#page-title.clearfix
  %h1= "Editar #{Razontiporesolucion.model_name.human}"
  = render :partial => "shared/breadcrumbs"

.show.widget
  .widget-header
    %h3 Editar Razontiporesolucion
  .widget-content
    = simple_form_for([:admin,@razontiporesolucion], :html => {:class => 'form-horizontal'}) do |form|
      = render :partial => 'form', :locals => {:form => form}
      .form-actions
        = buttons_update(admin_razonestiposresoluciones_path)