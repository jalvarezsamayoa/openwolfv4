#page-title.clearfix
  %h1= "Nuevo #{Documentoclasificacion.model_name.human}"
  = render :partial => "shared/breadcrumbs"

.show.widget
  .widget-header
    %h3 Nueva Clasificación Documental
  .widget-content
    = simple_form_for([:admin,@documentoclasificacion], :html => {:class => 'form-horizontal'}) do |form|
      = render :partial => 'form', :locals => {:form => form}
      .form-actions
        = buttons_create(admin_documentoclasificaciones_path)