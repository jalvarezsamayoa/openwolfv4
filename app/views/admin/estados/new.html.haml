#page-title.clearfix
  %h1= "Nuevo #{Estado.model_name.human}"
  = render :partial => "shared/breadcrumbs"

.show.widget
  .widget-header
    %h3 Nuevo Estado
  .widget-content
    = simple_form_for([:admin,@estado], :html => {:class => 'form-horizontal'}) do |form|
      = render :partial => 'form', :locals => {:form => form}
      .form-actions
        = buttons_create(admin_estados_path)
