#page-title.clearfix
  %h1= "Nuevo #{Municipio.model_name.human}"
  = render :partial => "shared/breadcrumbs"

.show.widget
  .widget-header
    %h3 Nuevo Municipio
  .widget-content
    = simple_form_for([:admin,@municipio], :html => {:class => 'form-horizontal'}) do |form|
      = render :partial => 'form', :locals => {:form => form}
      .form-actions
        = buttons_create(admin_municipios_path)