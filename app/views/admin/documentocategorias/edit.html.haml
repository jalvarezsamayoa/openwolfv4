#page-title.clearfix
  %h1= "Editar #{Documentocategoria.model_name.human}"
  = render :partial => "shared/breadcrumbs"

.show.widget
  .widget-header
    %h3 Editar Categoría Documental
  .widget-content
    = simple_form_for([:admin,@documentocategoria], :html => {:class => 'form-horizontal'}) do |form|
      = render :partial => 'form', :locals => {:form => form}
      .form-actions
        = buttons_update(admin_documentocategorias_path)
