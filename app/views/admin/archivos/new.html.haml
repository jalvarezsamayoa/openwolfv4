#page-title.clearfix
  %h1= "Nuevo #{Archivo.model_name.human}"
  = render :partial => "shared/breadcrumbs"

.show.widget
  .widget-header
    %h3 Nuevo Archivo
  .widget-content
    = simple_form_for([:admin,@archivo], :html => {:class => 'form-horizontal'}) do |form|
      = render :partial => 'form', :locals => {:form => form}
      .form-actions
        = buttons_create(admin_archivos_path)