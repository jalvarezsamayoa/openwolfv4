#page-title.clearfix
  %h1= "Nuevo #{Tiporesolucion.model_name.human}"
  = render :partial => "shared/breadcrumbs"

.show.widget
  .widget-header
    %h3 Nuevo Tiporesolucion
  .widget-content
    = simple_form_for(@tiporesolucion, :html => {:class => 'form-horizontal'}) do |form|
      = render :partial => 'form', :locals => {:form => form}
      .form-actions
        = buttons_create(tiposresoluciones_path)