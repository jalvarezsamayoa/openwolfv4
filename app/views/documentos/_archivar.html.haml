.mantform
  %h2
    Archivar Documento

  = simple_form_for @documento do |form|


    = form.inputs do
      = form.input :archivo, :collection => @archivos, :include_blank => false

    = submit_popup("Archivar")
    = close_dialog("archivar-dialog")

