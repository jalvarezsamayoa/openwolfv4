- content_for :head do
  = javascript_include_tag 'combobox.js', 'documentos.js', 'jquery.ui.datepicker-es.js'
.mantform
  %h2
    Nuevo #{Documento.model_name.human}
  = simple_form_for @documento, :url => documentos_path(@institucion) do |form|

    = render :partial => 'form', :locals => {:form => form}
    = buttons_create(documentos_path(@institucion))
