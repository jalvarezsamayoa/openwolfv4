.mantform
  %h2
    Editar #{Tipomensaje.model_name.human}
  = simple_form_for @tipomensaje do |form|

    = render :partial => 'form', :locals => {:form => form}
    = buttons_update(tipomensajes_path)
